version: '3'

tasks:
  build:
    desc: "Compile all reports into web/data.json and build static dashboard"
    cmds:
      - node web/builder.js

  serve:
    desc: "Run local Node.js preview server for the web dashboard"
    cmds:
      - cd web && node cli.js --serve
